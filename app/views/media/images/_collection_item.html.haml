.collection_thumb{:id => "collection_medium_#{collection_item.id}"}
  =medium_remove_from_collection_button media_set, collection_item, :div_prefix => 'collection'
  =medium_image_tag(collection_item, :thumbnail)
